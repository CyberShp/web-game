name: Bootstrap from revival-archives

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/bootstrap-from-revival-archives.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Preserve target-only files
        run: |
          mkdir -p /tmp/revival-baseline
          cp design/campaign/CURRENT-LOCKED-BASELINE.md /tmp/revival-baseline/CURRENT-LOCKED-BASELINE.md
          cp .github/workflows/bootstrap-from-revival-archives.yml /tmp/revival-baseline/bootstrap.yml

      - name: Checkout organization source
        uses: actions/checkout@v4
        with:
          repository: Cer6erusovo/revival-archives
          ref: main
          path: __source

      - name: Mirror source tree except workflows
        run: |
          rsync -a --delete \
            --exclude '.git/' \
            --exclude '__source/' \
            --exclude '.github/workflows/' \
            __source/ ./
          rm -rf __source
          mkdir -p design/campaign .github/workflows
          cp /tmp/revival-baseline/CURRENT-LOCKED-BASELINE.md design/campaign/CURRENT-LOCKED-BASELINE.md
          cp /tmp/revival-baseline/bootstrap.yml .github/workflows/bootstrap-from-revival-archives.yml
          rm -f .init

      - name: Commit mirrored repository
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if git diff --cached --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git commit -m 'chore: mirror revival-archives project baseline'
          git push origin HEAD:main
